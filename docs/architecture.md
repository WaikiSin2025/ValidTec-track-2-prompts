# Track 2 Reference Architecture

```text
5. Improve   Evaluation set | failure taxonomy | run telemetry | promotion gate | rollback log
4. Operate   Feature brief | risk map | automation review | execution evidence | release packet
3. Remember  CLAUDE.md | testing skills | known issues | commands | owners | review dates
2. Context   Source authority | freshness | context packet | token budget | source IDs
1. Prompt    System policy | task brief | output contract | approved examples
```

This repository implements a compact deterministic slice of those layers so each control can be inspected and tested in CI.
